# Part 1: Combining Multiple Operators

# Task 1.1: Multi-Operation Update
db.products.updateOne(
  { sku: "CAMERA-CANON-EOS-R5" },
  {
    $inc: { price: 50 },
    $addToSet: { tags: "premium" },
    $set: { status: "active" },
    $currentDate: { lastUpdated: true }
  }
)

{
  acknowledged: true,
  insertedId: null,
  matchedCount: 1,
  modifiedCount: 1,
  upsertedCount: 0
}

# Task 1.2: Update Nested and Array Together
db.users.updateOne(
  { email: "user@example.com" },
  {
    $set: { "address.city": "Boston" },
    $addToSet: { interests: "photography" },
    $inc: { loginCount: 1 }
  }
)

{
  acknowledged: true,
  insertedId: null,
  matchedCount: 1,
  modifiedCount: 1,
  upsertedCount: 0
}

# Part 2: Conditional Update Operators

# Task 2.1: $min Operator
db.products.updateOne(
  { sku: "CAMERA-CANON-EOS-R5" },
  { $min: { price: 899 } }
)

{
  acknowledged: true,
  insertedId: null,
  matchedCount: 1,
  modifiedCount: 1,
  upsertedCount: 0
}

# Task 2.2: $max Operator
db.products.updateOne(
  { sku: "CAMERA-CANON-EOS-R5" },
  { $max: { quantity: 100 } }
)

{
  acknowledged: true,
  insertedId: null,
  matchedCount: 1,
  modifiedCount: 1,
  upsertedCount: 0
}

# Task 2.3: Price Floor and Ceiling
db.products.updateOne(
  { sku: "LAPTOP-DELL-XPS13" },
  {
    $max: { price: 49.99 },
    $min: { discountPercent: 90 }
  }
)

{
  acknowledged: true,
  insertedId: null,
  matchedCount: 1,
  modifiedCount: 1,
  upsertedCount: 0
}

# Part 3: $setOnInsert with Upsert

# Task 3.1: Create or Update with Defaults
db.users.updateOne(
  { email: "new.user@example.com" },
  {
    $set: {
      name: { first: "New", last: "User" },
      email: "new.user@example.com",
      status: "active"
    },
    $setOnInsert: {
      createdAt: new Date(),
      role: "customer"
    }
  },
  { upsert: true }
)

{
  acknowledged: true,
  insertedId: null,
  matchedCount: 1,
  modifiedCount: 1,
  upsertedCount: 0
}

# Task 3.2: Track First and Last Seen
db.users.updateOne(
  { email: "user@example.com" },
  {
    $set: { lastSeen: new Date() },
    $setOnInsert: { firstSeen: new Date() }
  },
  { upsert: true }
)

{
  acknowledged: true,
  insertedId: null,
  matchedCount: 1,
  modifiedCount: 1,
  upsertedCount: 0
}

# Part 4: Bulk Write Operations

# Task 4.1: Multiple Updates in One Call
db.products.bulkWrite([
  {
    updateOne: {
      filter: { sku: "PRODUCT-001" },
      update: { $set: { status: "active" } }
    }
  },
  {
    updateOne: {
      filter: { sku: "PRODUCT-002" },
      update: { $inc: { quantity: 10 } }
    }
  },
  {
    insertOne: {
      document: {
        sku: "PRODUCT-003",
        name: "New Product",
        price: 99.99
      }
    }
  }
])

{
  acknowledged: true,
  insertedCount: 1,
  insertedIds: {
    '2': ObjectId('68f8a47870a1b06a995cde1b')
  },
  matchedCount: 0,
  modifiedCount: 0,
  deletedCount: 0,
  upsertedCount: 0,
  upsertedIds: {}
}

# Task 4.2: Bulk Upsert
db.products.bulkWrite([
  {
    updateOne: {
      filter: { sku: "SKU-001" },
      update: { $set: { name: "Product 1", price: 29.99 } },
      upsert: true
    }
  },
  {
    updateOne: {
      filter: { sku: "SKU-002" },
      update: { $set: { name: "Product 2", price: 39.99 } },
      upsert: true
    }
  }
])

{
  acknowledged: true,
  insertedCount: 0,
  insertedIds: {},
  matchedCount: 0,
  modifiedCount: 0,
  deletedCount: 0,
  upsertedCount: 2,
  upsertedIds: {
    '0': ObjectId('68f8a47fcfb6b96bf81246ac'),
    '1': ObjectId('68f8a47fcfb6b96bf81246ad')
  }
}

# Part 5: Atomic Operations

# Task 5.1: Increment and Return
const result = db.counters.findAndModify({
  query: { _id: "orderNumber" },
  update: { $inc: { sequence: 1 } },
  new: true,
  upsert: true
})
const nextOrderNumber = result.sequence

Risposta = 0 

# Task 5.2: Reserve Inventory
const reserve = db.products.updateOne(
  { sku: "LAPTOP-DELL-XPS13", quantity: { $gte: 5 } },
  { $inc: { quantity: -5 } }
)
if (reserve.modifiedCount === 0) throw new Error("Insufficient stock")

Risposta = 0

# Part 6: Conditional Logic with $cond

# Task 6.1: Set Status Based on Quantity
db.products.updateMany(
  {},
  [
    {
      $set: {
        status: {
          $cond: {
            if: { $gt: ["$quantity", 0] },
            then: "in-stock",
            else: "out-of-stock"
          }
        }
      }
    }
  ]
)

{
  acknowledged: true,
  insertedId: null,
  matchedCount: 6,
  modifiedCount: 6,
  upsertedCount: 0
}

# Part 7: Real-World Patterns

# Pattern 1: Shopping Cart - Add or Update Quantity
const userCart = db.users.findOne({
  email: "user@example.com",
  "cart.productId": productId
})
if (userCart) {
  db.users.updateOne(
    { email: "user@example.com", "cart._id": productId },
    { $inc: { "cart.$.quantity": 1 } }
  )
} else {
  db.users.updateOne(
    { email: "user@example.com" },
    {
      $push: { cart: { productId, quantity: 1, addedAt: new Date() } }
    }
  )
}

Risposta = Error

# Pattern 2: Product View Counter
db.products.updateOne(
  { sku: "CAMERA-CANON-EOS-R5" },
  {
    $inc: { viewCount: 1 },
    $set: { lastViewed: new Date() },
    $setOnInsert: { firstViewed: new Date() }
  },
  { upsert: true }
)

{
  acknowledged: true,
  insertedId: null,
  matchedCount: 1,
  modifiedCount: 1,
  upsertedCount: 0
}

# Pattern 3: Average Rating Calculation
db.products.updateOne(
  { sku: "LAPTOP-DELL-XPS13" },
  { $inc: { "rating.count": 1, "rating.total": 5 } }
)

{
  acknowledged: true,
  insertedId: null,
  matchedCount: 1,
  modifiedCount: 1,
  upsertedCount: 0
}

const product = db.products.findOne({ sku: "LAPTOP-DELL-XPS13" })
const average = product.rating.total / product.rating.count
db.products.updateOne({ sku: "LAPTOP-DELL-XPS13" }, { $set: { "rating.average": average } })

{
  acknowledged: true,
  insertedId: null,
  matchedCount: 1,
  modifiedCount: 0,
  upsertedCount: 0
}

# Pattern 4: Soft Delete with Timestamp
db.users.updateOne(
  { email: "user@example.com" },
  { $set: { status: "deleted", deletedAt: new Date(), deletedBy: adminUserId } }
)

Risposta = 0

# Pattern 5: Versioning
db.products.updateOne(
  { sku: "LAPTOP-DELL-XPS13" },
  { $set: { name: "Updated Name", price: 1399.99 }, $inc: { __v: 1 }, $currentDate: { updatedAt: true } }
)

{
  acknowledged: true,
  insertedId: null,
  matchedCount: 1,
  modifiedCount: 1,
  upsertedCount: 0
}

# Part 8: Error Handling and Validation

# Task 8.1: Check Update Results
const resCheck = db.products.updateOne({ sku: "NON-EXISTENT" }, { $set: { status: "active" } })
if (resCheck.matchedCount === 0) console.log("Product not found")
else if (resCheck.modifiedCount === 0) console.log("No changes made")
else console.log("Updated successfully")

Risposta = Not Found

# Task 8.2: Validate Before Update
const adminUser = db.users.findOne({ email: "user@example.com", role: "admin" })
if (!adminUser) throw new Error("Unauthorized")
db.products.updateOne(...)

Risposta = Error

# Part 9: Performance Considerations

# Task 9.1: Batch Updates vs Individual
products.forEach(p => db.products.updateOne({ _id: p._id }, { $set: { status: "active" } }))
db.products.updateMany({ _id: { $in: productIds } }, { $set: { status: "active" } })

Risposta = Error

# Task 9.2: Update with Index
db.products.updateOne({ sku: "LAPTOP-DELL-XPS13" }, { $set: { status: "active" } })

{
  acknowledged: true,
  insertedId: null,
  matchedCount: 1,
  modifiedCount: 1,
  upsertedCount: 0
}

db.products.updateOne({ name: "Dell XPS 13" }, { $set: { status: "active" } })

{
  acknowledged: true,
  insertedId: null,
  matchedCount: 0,
  modifiedCount: 0,
  upsertedCount: 0
}

# Validation & Testing
const before = db.products.findOne({ sku: "TEST-SKU" })
db.products.updateOne({ sku: "TEST-SKU" }, { $inc: { price: 10 }, $addToSet: { tags: "new-tag" }, $set: { status: "active" } })
const after = db.products.findOne({ sku: "TEST-SKU" })
assert(after.price === before.price + 10)
assert(after.tags.includes("new-tag"))
assert(after.status === "active")

Risposta = Error

# Advanced Challenges (Optional)

# Challenge 1: Implement Like System
function toggleLike(productId, userId) {
  const p = db.products.findOne({ _id: productId, likedBy: userId })
  if (p) db.products.updateOne({ _id: productId }, { $pull: { likedBy: userId }, $inc: { likeCount: -1 } })
  else db.products.updateOne({ _id: productId }, { $addToSet: { likedBy: userId }, $inc: { likeCount: 1 } })
}

Risposta = 0

# Challenge 2: Inventory Management
db.products.updateOne(
  { sku: "PRODUCT-SKU", availableQuantity: { $gte: quantity } },
  { $inc: { availableQuantity: -quantity, reservedQuantity: quantity }, $push: { reservations: { userId, quantity, expiresAt: new Date(Date.now()+900000) } } }
)

Risposta = 0

db.products.updateMany(
  { "reservations.expiresAt": { $lt: new Date() } },
  { $pull: { reservations: { expiresAt: { $lt: new Date() } } } }
)

{
  acknowledged: true,
  insertedId: null,
  matchedCount: 0,
  modifiedCount: 0,
  upsertedCount: 0
}

